# import BusinessManagement from './BusinessManagementFragment.graphql'

mutation UpdateBusinessManagement($input: BusinessManagementInput!) {
  updateBusinessManagement(input: $input) {
    ...BusinessManagement
  }
}
